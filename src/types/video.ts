export type Video = {
  id: string;
  title: string;
  description: string;
  thumbnail: string;
  videoUrl: string;
  duration: string;
  views: number;
  tags: string[];
  createdAt: string;
  published: boolean;
};